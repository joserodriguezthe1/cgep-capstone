exclude:
  - id: aws-s3-encryption-customer-key
    justification: "Lab 2.3 uses AES256 SSE which satisfies SC-28. CMK is enforced at the GCP layer via Lab 2.4 CMEK. AWS CMK would be added in a production deployment."